[build]
  command = "npm run build"
  publish = "dist"

# This routing rule ensures that Netlify works properly with Vue Router in HTML5 History mode
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
